import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Administration | MyProfilCookie',
  description: 'Interface d\'administration du portfolio',
}

export default function AdminPage() {
  return null
} 